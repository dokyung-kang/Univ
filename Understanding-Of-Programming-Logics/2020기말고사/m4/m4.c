#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int is_in(char w[], char ch);
int main(void)
{
	int i;
	char str[20];
	char a;

	scanf("%d", a);
	
	printf("%d", is_in(str, a));

}
int is_in(char w[], char ch)
{
	int i;

	for (i = 0; w[i] != '\0'; i++)
	{
		if ()
	}
}